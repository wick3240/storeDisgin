<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wick.store.repository.SubscribeWorkflowApproveMapper">


    <update id="updateSubscribeStatus">
        update subscribe_workflow_approve set
        node_approval_status=#{approveStatus},node_approval_time=#{approvalTime},node_approval_user=#{approveUser},node_approval_reason=#{approvalReason}
        where subscribe_code=#{subscribeCode} and wf_handle_node_id=#{nodeId} and cid=#{cid}
    </update>
    <select id="selectBySubCodesAndNodeId"
            resultType="com.wick.store.domain.entity.SubscribeWorkflowApproveEntity">
        select * from
        subscribe_workflow_approve where subscribe_code=#{subscribeCode}
    </select>
</mapper>